# TOR-Routed-Shell
A small cross-platform Python utility that launches a new shell (terminal window) with all proxy environment variables preconfigured to route traffic through a local Tor SOCKS5 proxy.
